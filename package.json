{
  "name": "admin-auth-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "ADMIN_AUTH_COOKIE_SECURE=false node --watch server/index.js",
    "test": "node --test tests/*.test.js",
    "accounts:preview": "node scripts/import-legacy-accounts.js",
    "test:integration": "node --test integration/*.test.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "express": "^4.21.2"
  }
}
