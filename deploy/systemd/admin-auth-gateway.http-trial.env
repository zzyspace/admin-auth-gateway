# TEMPORARY: remove this file when HTTPS is enabled.
# HTTP cannot use Secure or __Host- cookies. Keep this mode as short-lived as
# possible because credentials and session cookies travel without TLS.
ADMIN_AUTH_COOKIE_SECURE=false
ADMIN_AUTH_COOKIE_NAME=admin_session
ADMIN_AUTH_SESSION_TTL_SECONDS=604800
